---
- name: "Run Role"
  hosts: all
  become: true
  gather_facts: true

  vars_files:
    - "~/rhisbuilder_vault.yml"

  tasks:

    - name: "Apply role {{ role_name }}"
      ansible.builtin.include_role:
        name: "{{ role_name }}"
